import { Box } from '@mui/material';
import { grey } from '@mui/material/colors';

const ExpDividerUI = () => (
  <Box width={25} sx={{ borderBottom: `2px solid ${grey[400]}`, mb: 2 }} />
);

export default ExpDividerUI;
